import { Navbar } from "renderer/components/NavBar";

export const Investment = () => {
    return (
        <main>
            <Navbar selected={'investment'} />
            <h1>Investment</h1>
        </main>
    )
}